# Archived — P0016 corpus ingest (removed)

These scripts pulled P0016 `/api/ai/corpus/export` into P0020 notes (`chatcenter-corpus` domain).

**Status:** Deprecated. P0016 chat storage is standalone on VPS; P0020 only shares Hub UI/design patterns with P0016, not data pipelines.

Files kept for reference only — do not run in production.
