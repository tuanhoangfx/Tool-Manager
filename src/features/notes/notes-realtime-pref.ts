/** Realtime UI refresh is always on (Notes list + open note + Cookie Auto). */
export function readNotesRealtimeUiRefresh(): boolean {
  return true;
}

export function useNotesRealtimeUiRefresh(): boolean {
  return true;
}
