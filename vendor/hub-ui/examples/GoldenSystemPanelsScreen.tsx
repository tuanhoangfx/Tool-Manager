/**
 * HUB_UI_SCAFFOLD — system-panels template (System sub-tabs).
 * Generated by: node Tool/scripts/sync-hub-ui-screen.cjs P00xx system
 *
 * TODO: wire SystemTabSubNav, visited panels, HubTabChrome per sub-tab.
 */
import { useState } from "react";
import { HubPanel, HubTabChrome } from "@tool-workspace/hub-ui";

// TODO: import SystemTabSubNav / panel components from app

export function GoldenSystemPanelsScreen() {
  const [activeTab, setActiveTab] = useState("overview");

  return (
    <div className="flex min-h-0 flex-1 flex-col">
      <nav className="px-6 py-2 text-xs text-[var(--muted)]">TODO: SystemTabSubNav</nav>
      <HubTabChrome
        header={
          <div className="px-6 py-3 text-sm text-[var(--muted)]">
            TODO: SystemTabHeader — title, refresh, DisplayPrefs scope=&quot;tab&quot;
          </div>
        }
        filters={null}
        sectionRuleLabel={activeTab}
      >
        <HubPanel title="Panel body" className="mx-6">
          <p className="text-sm text-[var(--muted)]">
            Active tab: {activeTab}. Clone from Tool/P0004-Tool-Hub/src/features/system-hub/SystemHubScreen.tsx
          </p>
        </HubPanel>
      </HubTabChrome>
    </div>
  );
}
