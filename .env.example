# Tool Manager (P0020) — copy to .env.local (never commit secrets)

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Optional: GitHub API for Library refresh (read-only token recommended)
# VITE_GITHUB_TOKEN=
