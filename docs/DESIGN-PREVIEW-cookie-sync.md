# Cookie Sync Design Preview

URL: `http://127.0.0.1:5177/?screen=system&stab=template&dtpl=cookie-sync`

Five preview directions:

- `V1 Source Locked Table`: dense Hub-style table with source lock column.
- `V2 Command Center`: operator dashboard with source and target cards.
- `V3 Route Matrix`: route x browser permission map.
- `V4 Modal Operations`: clean table with add/edit/delete/source setup in modal.
- `V5 Extension Mirror`: popup-inspired visual language for the extension UI.

These are static mockups only. Production Web/Extension redesign should promote one selected variant or a deliberate mix after review.
